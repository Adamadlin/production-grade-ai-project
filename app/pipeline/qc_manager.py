from app.utils.types import Chunk, QCEval

class QCManager:
    # Stubs — replace with real checks later
    def evaluate(self, chunk: Chunk) -> QCEval:
        return {"toxicity": 0.0, "pii": 0.0, "bias": 0.0}